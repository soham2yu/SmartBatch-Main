## Packages
recharts | Dashboard analytics charts and data visualization
framer-motion | Page transitions and animations

## Notes
Assuming API definitions from @shared/routes are available
Assuming schemas from @shared/schema are available
File uploads use FormData (no Content-Type header in fetch so browser sets boundary)
Dark mode is the default and only theme for this industrial dashboard
